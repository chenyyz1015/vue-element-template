If the current project has `CLAUDE.md`, read it as the primary project instructions document. This file contains the full technical stack, conventions, directory structure, and coding rules for the Vue 3 + TypeScript project.

If `CLAUDE.md` references `.claude/rules/` files (code-style, api-conventions, css-naming, directives), read the corresponding `.codex/rules/` copies to understand the project's detailed conventions.
