import type { Config } from "prettier";

const config: Config = {
  printWidth: 120,
  semi: true,
  singleQuote: false,
  useTabs: false,
  bracketSpacing: true,
  trailingComma: "es5",
};

export default config;
