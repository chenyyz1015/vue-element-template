# Issue 闭环专用（复制为 .env.sentry-resolve.local，勿提交仓库）
# Organization Token 在 UI 中仅 org:ci，不能 resolve；此处使用 User Auth Token。

SENTRY_RESOLVE_AUTH_TOKEN=<user-token-with-event-write>
# SENTRY_URL、SENTRY_ORG 可省略（沿用 .env.sentry-build-plugin）
