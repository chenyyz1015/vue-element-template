---
description: UX 设计评审（impeccable）。评估 hierarchy、clarity、认知负荷、anti-slop。
argument-hint: [page-or-component-path]
---

# UX 设计评审 (/critique)

对 $ARGUMENTS（未指定则当前 UI 变更）执行 **/impeccable critique**。

## 执行步骤

1. 读 `.cursor/skills/impeccable/project-bridge.md`
2. 读 `.cursor/skills/impeccable/SKILL.md` + `reference/critique.md`
3. 加载上下文（同 audit）
4. 浏览器走完整用户路径
5. 输出 UX 评分与 P0/P1

建议顺序：`/critique` → `/audit` → `/polish` → `/audit` 复验
