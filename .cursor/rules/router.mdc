---
description: 路由目录约定与 RBAC 权限控制（路由级 + 按钮级）
globs:
  - src/router/**
  - src/stores/modules/user.ts
  - src/stores/modules/permission.ts
  - src/composables/usePermission.ts
  - src/directives/**
  - src/utils/permission.ts
alwaysApply: false
---

# 路由与 RBAC

- 根目录 `src/router/`：主路由、守卫、常量路由；业务模块放 `router/modules/`（**kebab-case**）
- **constantRoutes**：登录/403/404 + 公开页面；**asyncRoutes**：登录后按权限 `addRoute`
- Store：`useUserStore`（登录、roles、permissions）· `usePermissionStore`（动态路由）
- 路由级：`meta.requiresAuth` / `meta.roles` / `meta.permissions`
- 按钮级：`usePermission()` · `v-permission="'code'"` · `v-permission.all="['a','b']"`
- 禁止业务代码绕过 `usePermissionStore.generateRoutes` 直接 `addRoute`
- 详细规范见 `.claude/rules/router.md`
