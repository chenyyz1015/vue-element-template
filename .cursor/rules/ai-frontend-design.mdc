---
description: AI 前端设计规范。涉及 UI 设计、页面/组件创建、视觉稿、audit/polish 时适用。
globs:
  - src/views/**
  - src/components/**
  - design/**
  - design-system/**
alwaysApply: false
---

# AI 前端设计规范

## 工具

| 工具 | 角色 | Phase |
|------|------|-------|
| ui-ux-pro-max | 设计战略顾问 | 1 战略规划 |
| pencil | 视觉协同与执行 | 2–3 视觉 + 代码 |
| impeccable | 代码级设计监工 | 4 质量审计 |

## 工作流

1. **Phase 1**：`ui-ux-pro-max` → `design-system/` + `design/briefs/`
2. **Phase 2**：Pencil MCP → `design/pages/*.pen`
3. **Phase 3**：Vue 实现 + `pencil-sync.md` + `npm run build`
4. **Phase 4**：`/audit` → `/polish` → gate（≥14/20，无 P0/P1）

完整流程：`.cursor/skills/ai-frontend-design-workflow/SKILL.md`

## 触发规则

- 新建页面/组件/UI 改版 → 完整 Phase 1–4
- 用户明确只要代码 → Phase 1 → 3 → 4
- 仅审查/精修 → Phase 4（`.cursor/skills/impeccable/SKILL.md`）
- 多页面任务 → invoke `design-director` Agent

## Phase 4 命令

- `/audit` 或 `/impeccable audit` — 技术质量审计
- `/critique` 或 `/impeccable critique` — UX 评审
- `/polish` 或 `/impeccable polish` — 交付前精修
- 完整 23 命令见 `.cursor/skills/impeccable/SKILL.md`

上下文：`IMPECCABLE_CONTEXT_DIR=design-system` · 桥接说明见 `project-bridge.md`

## 设计产物

- `design-system/MASTER.md` — 唯一真相源
- `design/briefs/` — 页面简报
- `design/pages/` — Pencil 稿（仅 MCP 访问）

## 禁止

- 无 Brief 大规模写 UI
- Phase 3 未 build 通过就交付
- 跳过 Phase 4 gate（除非用户豁免）
- 直接 Read/Grep `.pen` 文件
