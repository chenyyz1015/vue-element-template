---
description: AI 前端设计规范。涉及 UI 设计、页面/组件创建、视觉稿、audit/polish 时适用。
globs:
  - src/views/**
  - src/components/**
  - design/**
  - design-system/**
alwaysApply: false
---

# AI 前端设计规范

- **Phase 1–4**：ui-ux-pro-max → pencil → Vue（`useThemeColor` / `useThemeMode`）→ impeccable audit/polish
- **Gate**：Audit ≥ 14/20，无 P0/P1，`npm run build` 通过
- **主题**：`THEME.md` + **`TOKENS.md`**（双模式语义色）· **项目名**：`VITE_APP_NAME`（见 `PROJECT.md`）
- **主编排**：`.cursor/skills/ai-frontend-design-workflow/SKILL.md`
- **Pencil**：`.cursor/skills/pencil-design-workflow/SKILL.md` · `pencil-sync.md`
- **质量**：`.cursor/skills/impeccable/SKILL.md` · `project-bridge.md`
- **Agents**：`.cursor/agents/design-director.md` · `design-inspector.md`
- **Commands**：`.cursor/commands/audit.md` · `polish.md` · `critique.md`
- **静态资源**：页面/组件媒体统一 `src/assets/`（`icons/` `images/` `fonts/` `videos/` `audio/`）；Pencil `export_nodes` 产出按类型落盘，Vue 用 `@/assets/...`
- **design/**：仅 `design/README.md` 入库；`briefs/`、`pages/`、`components/`、`scripts/` 为本地临时，勿提交
- **Pencil 脚本**：创建 `.pen` 的生成脚本仅放 `design/scripts/`（勿放 skills / 根 `scripts/` / `src/`）；见 `design/README.md`
- **禁止**：无 Brief 大规模写 UI · 跳过 Phase 4 · 直接 Read/Grep `.pen` · 在 `views/`/`components/` 下自建资源目录 · 提交 `design/` 临时文件
- 详细规范见 `.claude/rules/ai-frontend-design.md`
